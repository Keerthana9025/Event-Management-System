<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.24.xsd">

    <changeSet id="3" author="venka">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="registrations"/>
            </not>
        </preConditions>

        <createTable tableName="registrations">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="participant_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="event_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="registered_at" type="TIMESTAMP"/>
            <column name="status" type="VARCHAR(50)" defaultValue="CONFIRMED"/>
        </createTable>

        <addUniqueConstraint tableName="registrations"
                             columnNames="participant_id, event_id"
                             constraintName="uk_registration_participant_event"/>

        <addForeignKeyConstraint baseTableName="registrations"
                                 baseColumnNames="participant_id"
                                 constraintName="fk_reg_participant"
                                 referencedTableName="participants"
                                 referencedColumnNames="id"/>

        <addForeignKeyConstraint baseTableName="registrations"
                                 baseColumnNames="event_id"
                                 constraintName="fk_reg_event"
                                 referencedTableName="events"
                                 referencedColumnNames="id"/>
    </changeSet>

</databaseChangeLog>
